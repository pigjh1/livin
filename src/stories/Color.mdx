import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';

export const grayscaleColors = [
  "#f9fafb", // gray-50
  "#f3f4f6", // gray-100
  "#e5e7eb", // gray-200
  "#d1d5db", // gray-300
  "#9ca3af", // gray-400
  "#6b7280", // gray-500
  "#4b5563", // gray-600
  "#374151", // gray-700
  "#1f2937", // gray-800
  "#111827", // gray-900
  "#030712", // gray-950 (optional deepest)
];

export const primaryColors = ['#9A450C'];
export const secondaryColors = ['#BCAAA4'];

export const colorsListObj = {
  primary: primaryColors,
  secondary: secondaryColors,
  grayscale: grayscaleColors,
};

export function getContrastColor(hex) {
  const hexCode = hex.charAt(0) === '#' ? hex.substr(1, 6) : hex;
  const hexR = parseInt(hexCode.substr(0, 2), 16);
  const hexG = parseInt(hexCode.substr(2, 2), 16);
  const hexB = parseInt(hexCode.substr(4, 2), 16);
  const contrastRatio = (hexR + hexG + hexB) / (255 * 3);
  return isNaN(contrastRatio) || contrastRatio >= 0.5 ? '#000' : '#FFF';
}

<Meta title="Foundation/Color" />

# Color

<ColorPalette>
  <ColorItem title="Gray Scale" colors={grayscaleColors} />
  <ColorItem title="Primary Color" colors={primaryColors} />
  <ColorItem title="Secondary Color" colors={secondaryColors} />
</ColorPalette>

